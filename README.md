# Chef Network Platform

Interactive viewer for a chef influence network. The dataset is **not public**:
`data.enc` is AES-256-GCM ciphertext and is decrypted only in the visitor's
browser with an access key shared privately by the author. Without a key, the
viewer can still load JSON files from the visitor's own computer (nothing is uploaded).
